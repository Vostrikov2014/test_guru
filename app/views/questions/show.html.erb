<h3>Test: <%= @question.test.title %></h3>

<h2>Question: <%= @question.body %></h2>

<h2>Answers</h2>
<%= link_to 'Новый ответ', new_question_answer_path(@question) %>

<ol>
  <%= render @question.answers %>
</ol>

<p>
<%= link_to 'Редактировать', edit_question_path(@question) %> |
<%= link_to 'Удалить', question_path(@question), method: :delete, data: {confirm: "Вы согласны?" } %> |
<%= link_to 'Назад', test_path(@question.test) %>
</p>