<h1 class="mb-4 mt-4 text-center"><%= t('.all_badges') %></h1>

<p>
  <%= link_to t('.new_badge'), new_admin_badge_path %>
</p>

<%= render 'shared/errors', resources: @badge %>

<table class="table">
  <tr>
    <th><%= t('.title')%></th>
    <th><%= t('.image')%></th>
    <th><%= t('.rule')%></th>
    <th><%= t('.value')%></th>
  </tr>

  <tbody>
  <%= render @badges %>
  </tbody>
</table>
