<div class="container">
  <h1><%= t('.header') %></h1>

  <table class="table">
    <tr>
      <th><%= t('.question') %></th>
      <th><%= t('.gist_url') %></th>
      <th><%= t('.user') %></th>
    </tr>
    <%= render @gists %>
  </table>

  <%= link_to t('.back'), admin_tests_path %>
</div>
