
<!-- Исходно пишется так:
    I18n.t('tests.index.header')  можно использовать .header
    если структура ключа повторяет структуру каталогов (имена совпадают)
    КЛЮЧ в файле ru.yml
    ru:
      tests:
        index:
          header:-->
<h1 class="mb-4 mt-4 text-center"><%= t('.header') %></h1>

<table class="table">
  <tr>
    <th class="sort-by-title pointer">
      <%= t('.title') %>
      <%= octicon 'arrow-up', class: 'text-success hide' %>
      <%= octicon 'arrow-down', class: 'text-success hide' %>
    </th>
    <th><%= t('.level') %></th>
    <th><%= t('.count') %></th>
    <th><%= t('.actions') %></th>
  </tr>

  <%= render @tests %>
</table>
